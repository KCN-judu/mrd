# Final Complexity Report

Evidence baseline: `0752fce60d5a801173e963b4a1fb55c8a331949e`.

## Supported Statements

- Exactness and deterministic ordering are established on the recorded finite
  campaigns.
- Direct-grid parity avoids rank sorting and rank-map materialization on the
  finite integer-grid path, as shown by zero direct counters.
- The flow and geometry reports expose structural counters and exact
  certificates for their tested inputs.
- The permanent reference implementations remain available and are not hidden
  behind an unproved claim.

## Unproved Statements

The missing proposition is: given the event objects generated by the cited AN19
construction, prove an explicit upper bound on the number of reduced-event
equivalence classes and justify the ordering transformation used by the
implementation, with sufficient detail to derive the claimed runtime.

The formal SIAM version of Abraham--Neiman (DOI `10.1137/17M1115575`) was
checked and does not provide that conversion from original power-of-two
edge-length classes to exact reduced-event classes for
`c_x(u,v) = ell(u,v) + d(x,u) - d(x,v)`. Therefore the reduced-event bound,
hierarchy-wide amortization, priority-queue bound, AN19 runtime verification,
and any downstream theorem depending on them remain deferred P9.6a proof debt.

Finite counters, tests, CP-SAT agreement, and local timings do not close this
proof obligation. The backend must not be called `AlmostLinear` and must not
report `an19_runtime_verified: true`.
